# ===============================
# Python / Django
# ===============================

# โฟลเดอร์สภาพแวดล้อมเสมือน (Virtual Environment)
env/
venv/
.venv/

# ไฟล์ Bytecode ของ Python
__pycache__/
*.pyc
*.pyo
*.pyd

# ไฟล์ Distribution / Packaging
*.egg-info/
dist/
build/


# ===============================
# Database
# ===============================

# ไม่ควรเก็บไฟล์ฐานข้อมูล SQLite ไว้ใน Git เพราะเป็นข้อมูลเฉพาะเครื่อง
db.sqlite3
*.sqlite3


# ===============================
# Node.js / Frontend
# ===============================

# โฟลเดอร์ที่เก็บแพ็กเกจทั้งหมดของ Node.js (ขนาดใหญ่มาก)
node_modules/

# ไฟล์ Log ของ npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*


# ===============================
# Environment Variables
# ===============================

# ไฟล์ที่เก็บค่าตัวแปรสภาพแวดล้อม เช่น Secret Key, API Key
# เป็นข้อมูลลับ ห้าม commit ขึ้น Git เด็ดขาด
.env
.env.*
local_settings.py


# ===============================
# Static & Media Files
# ===============================

# โฟลเดอร์ที่เก็บไฟล์ที่ผู้ใช้อัปโหลด (เช่น รูปโปรไฟล์)
# ควรเก็บไว้นอก Git และจัดการด้วยบริการจัดเก็บไฟล์เช่น AWS S3
media/

# โฟลเดอร์ที่ Django รวบรวมไฟล์ static ทั้งหมดมาไว้ด้วยกัน
# เราจะสร้างโฟลเดอร์นี้ใหม่ทุกครั้งที่ deploy
staticfiles/


# ===============================
# Editor / IDE / OS
# ===============================

# การตั้งค่าของ VSCode
.vscode/

# ไฟล์ของ macOS
.DS_Store

# ไฟล์ของ Windows
Thumbs.db